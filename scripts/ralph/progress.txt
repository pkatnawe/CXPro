## Codebase Patterns
- Backend asset column additions: update _ASSET_COLUMNS string, add param to create_asset/update_asset signatures, add to INSERT columns+VALUES (bump $N), add to UPDATE SET (bump $N), update _asset_row helper in tests
- apply_*_migration.py pattern: psycopg2 + load_dotenv('../.env.local'), open migration file, execute, verify column exists
- typecheck for backend: not applicable (Python), run pytest instead
- frontend-kit primitives: import from `@/lib/frontend-kit`; all CSS classes are fk-* (never bp-*); styles auto-loaded via layout.tsx
- frontend-kit pattern: components use className prop pass-through + variant/size/color unions; no `any` types; `'use client'` at top

# Ralph Progress Log
Started: Sun May 31 20:03:10 CDT 2026
---

## 2026-06-01 - US-001
- Created `migrations/020_asset_vendor_name.sql` with `ALTER TABLE assets ADD COLUMN IF NOT EXISTS vendor_name TEXT;`
- Created `backend/apply_vendor_name_migration.py` mirroring apply_asset_registry_migration.py pattern
- Updated `_ASSET_COLUMNS` in `assets.py` to include `vendor_name`
- Added `vendor_name` param to `create_asset` (INSERT col+$11) and `update_asset` (UPDATE SET col+$9, shift nameplate to $10, id/project to $11/$12)
- Updated `CreateAssetRequest` and `UpdateAssetRequest` in `api.py` to include `vendor_name`; wired through to `assets_mod.create_asset` and `update_asset` kwargs
- Added `vendor_name` to `_asset_row` helper in `test_assets.py`; added `test_create_asset_with_vendor_name_round_trips` asserting round-trip through create+get
- All 31 tests pass
- **Learnings for future iterations:**
  - INSERT param count changes: adding vendor_name before nameplate_data shifts nameplate from $10→$11
  - UPDATE SET changes: vendor_name=$9, nameplate_data=$10, id=$11, project_id=$12
  - _serialize in api.py handles all dict values via `str(v) if hasattr(v, "hex") else v` — no special handling needed for TEXT columns
---

## 2026-06-01 - US-002
- Created `scripts/seed_demo_data.py` — standalone psycopg2 + dotenv script
- Accepts `--email` flag; resolves to auth.users.id, errors if not found
- Idempotent: DELETE cascades existing project named 'DC-12 Hudson Valley (demo)' before inserting
- Inserts: 1 Org (Hudson DC LLC demo), 1 Project, 4 DisciplineScopes, 1 participation
- Spaces: Building → Hall A/B/C → Row 1/2/3 under Hall B
- 5 AssetTypes: AHU, PDU, UPS, CRAH, Switchgear
- 16 Assets across halls with realistic tags/mfr/model/serial/vendor_name/nameplate_data
- Parent/child chains: SWG-A-01 → PDU-A-01 → UPS-A-01, SWG-B-01 → PDU-B-01/B-02 → UPS-B-01/B-02
- 2 Systems: 'Cooling — Chilled Water' (6 members), 'Power — Hall B' (5 members)
- 5 TestProcedureTemplates (L2/L3/L5) linked via asset_type_template_links
- 13 TestProcedureInstances with varied statuses (pending/in_progress/complete)
- Script prints project_id + clickable localhost URL
- Backend tests: 31 pass
- **Learnings for future iterations:**
  - Seed scripts live in scripts/ and use `load_dotenv('../.env.local')` (relative to scripts/ dir)
  - Org slug must be unique; use ON CONFLICT (slug) DO UPDATE to make idempotent
  - participations table needs a row for the user to satisfy RLS on all asset_registry tables
  - Spaces UNIQUE constraint is (project_id, parent_space_id, name) — same name under different parents is OK
  - test_procedure_instances CHECK: (asset_id IS NOT NULL) != (system_id IS NOT NULL) — exactly one must be set
---

## 2026-06-01 - US-003
- Created `frontend/src/lib/frontend-kit/index.tsx` — TypeScript ports of: WBox, WPill, WT, WH, WAvatar, WBar, WStamp, WIcon, WLiveDot, WSectionLabel, WBtn, WHeader, WSideNav, WFrame, plus bonus helpers WTabs, WKV, WKVGrid, WSkeleton, WEmpty
- Created `frontend/src/lib/frontend-kit/styles.css` — all fk-* classes, no bp-* collision; uses bp-* CSS variables for theme consistency
- Imported styles in `frontend/src/app/layout.tsx` (root layout, loaded once globally)
- Created `frontend/src/app/frontend-kit-preview/page.tsx` (// SCRATCH) — renders all primitives for visual QA at /frontend-kit-preview
- `npm run type-check` passes clean; `eslint` on new files: 0 errors, 1 warning (img element; acceptable for design kit)
- `frontend-design/wireframe-kit.jsx` does not exist — primitives were designed from scratch to match the component names and semantics described in the PRD acceptance criteria and the US-004/005 screen descriptions
- **Learnings for future iterations:**
  - Import kit components: `import { WBox, WPill, ... } from '@/lib/frontend-kit'`
  - All CSS vars are from `--bp-*` tokens (dark/light theme support is automatic)
  - WTabs, WKV, WKVGrid, WSkeleton, WEmpty are bonus helpers useful for US-004/005
  - WPill variant="chip" supports `active` boolean prop for filter chip toggles
  - WHeader takes `crumbs[]` array for breadcrumb row
---

## 2026-06-01 - US-004
- Rewrote `frontend/src/app/(app)/project/[id]/assets/page.tsx` using frontend-kit primitives (WFrame, WHeader, WPill, WT, WLiveDot, WSkeleton, WEmpty, WBox)
- Added `vendor_name: string | null` to `Asset` interface in `api.ts`; also added to `createAsset` and `updateAsset` params
- Created `frontend/src/contexts/asset_registry/derivePhase.ts` — pure function, returns 'pre-install' | 'L2' | 'L3' | 'L4' | 'L5'
- Created `frontend/src/contexts/asset_registry/derivePhase.test.ts` — 10 unit tests covering all PRD cases
- Deleted `frontend/src/contexts/asset_registry/ui.test.tsx` (old AssetList test file)
- Layout: WHeader crumbs, decorative ✦ AI bar (alert on Enter), action pills (+new/Import BIM/Export), filter chips (space/type/status), WLiveDot row count, dense table, skeleton loading rows, WEmpty state
- Phase derivation: loaded all instances for project in single call, grouped by asset_id, derivePhase() per row
- Space path: buildSpacePath() walks spaceMap to parent recursively, joins with ' / '
- Children count: computed from parent_asset_id scan of full asset list
- Note: ui.tsx kept intact (still used by spaces/systems/asset-types pages); full deletion in US-009
- type-check: clean; lint: 0 errors on new files; 23 asset_registry tests pass
- **Learnings for future iterations:**
  - `listInstances(projectId)` with no filter loads all instances — then group by asset_id client-side to avoid N+1
  - derivePhase() ignores L1 (not in LEVEL_ORDER); only L2-L5 rank
  - WPill variant="chip" + active boolean + onClick clearFilter() = filter chip toggle pattern
  - buildSpacePath() walks Map upward via parent_space_id; memoize spaceMap outside render
  - `useUrlFilters` returns `clearFilter` (single key) and `clearAll` — no need for `setFilter` for clearing
---
