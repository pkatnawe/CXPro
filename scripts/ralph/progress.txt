# Ralph Progress Log
Started: Mon May 26 23:28:43 CDT 2026

## Codebase Patterns
- Use psycopg2 (not psycopg) for database connections in migration apply scripts
- Apply scripts pattern: load_dotenv('../.env.local') to get DATABASE_URL
- Migrations use DO blocks with IF NOT EXISTS checks for idempotency
- Migration numbering continues sequentially (last was 016, so next is 017)
- PostgreSQL constraint names follow pattern: <table>_<column>_check
---

## Mon May 26 23:34:00 CDT 2026 - US-001
- Implemented DB migration to extend memberships.role CHECK constraint to support 6 canonical human roles
- Files changed:
  - Created migrations/017_extend_membership_roles.sql with idempotent migration
  - Created backend/apply_extend_roles_migration.py following existing apply script convention
- Migration successfully applied to dev database
- Extended role values: OCA, CM, cx_engineer, field_technician, design_engineer, owner_fm
- **Learnings for future iterations:**
  - Both memberships and pending_invitations tables have role CHECK constraints that need updating in sync
  - PostgreSQL requires dropping existing CHECK constraint before adding a new one
  - Use pg_constraint system catalog to query existing constraints
  - Migration verified that existing OCA and cx_engineer roles were preserved
---

## Mon May 25 23:38:39 CDT 2026 - US-002
- Implemented backend roles module with is_valid_role function and unit tests
- Files changed:
  - Created backend/roles.py with ROLES tuple, ROLE_LABELS dict, and is_valid_role() function
  - Created backend/tests/test_roles.py with comprehensive test coverage
- All 5 tests pass successfully
- Validated the 6 canonical role values: OCA, CM, cx_engineer, field_technician, design_engineer, owner_fm
- **Learnings for future iterations:**
  - Backend uses pytest for testing, tests are placed in backend/tests/ directory
  - Test files use sys.path.insert to import modules from parent directory
  - No specific type checking tool is configured (no mypy/pyright), basic Python syntax checking is sufficient
  - The 6 roles match exactly with the migration constraints
---

## Mon May 25 23:41:48 CDT 2026 - US-003
- Implemented backend permissions module with can_manage_team and can_create_project predicates
- Files changed:
  - Created backend/permissions.py with two permission predicate functions
  - Created backend/tests/test_permissions.py with 7 comprehensive test functions
- All 7 tests pass successfully (testing both predicates with all 6 roles plus invalid values)
- Both predicates return True for 'OCA' and 'CM', False for all other roles and invalid inputs
- **Learnings for future iterations:**
  - Backend permission predicates follow same pattern as roles module
  - Functions handle None, empty string, and non-string types defensively
  - Test coverage includes all 6 canonical roles plus edge cases
  - No specific Python type checker configured - basic syntax and pytest are sufficient
---

## Mon May 25 23:46:27 CDT 2026 - US-004
- Implemented backend invite endpoint to use roles + permissions modules
- Files changed:
  - Modified backend/invitation_service.py to import and use is_valid_role and can_manage_team
  - Replaced _verify_user_is_oca with more general _get_user_role method
  - Updated create_invitation to validate role via is_valid_role (400 if invalid)
  - Changed permission gate from "OCA only" to can_manage_team check
  - Created backend/tests/test_invites_endpoint.py with 4 test cases
- All 4 new tests pass successfully (CM can invite, invalid role rejected, non-manager cannot invite, OCA backwards compatible)
- **Learnings for future iterations:**
  - InvitationService lives in backend/invitation_service.py, not in main.py
  - The service method _get_user_role returns Optional[str] to handle None cases
  - Test mocking pattern: use AsyncMock for connection, Mock for Supabase client  
  - CM users can now invite team members along with OCAs
  - Role validation happens before permission checks to give clearer error messages
---

## Mon May 25 23:49:08 CDT 2026 - US-005
- Implemented frontend lib/roles module with isValidRole type guard and unit tests
- Files changed:
  - Created frontend/src/lib/roles.ts with ROLES readonly tuple, Role type union, ROLE_LABELS mapping, and isValidRole type guard
  - Created frontend/src/lib/__tests__/roles.test.ts with 9 comprehensive test cases
- All 9 tests pass under vitest
- TypeScript type checking passes with npm run type-check
- **Learnings for future iterations:**
  - Frontend uses vitest for testing, not jest
  - TypeScript check command is `npm run type-check` not `npm run typecheck`
  - Role labels follow human-readable format (e.g., "Owner's Commissioning Agent" for OCA)
  - Type guard pattern: check typeof === 'string' first, then inclusion in array
  - Frontend role values match backend exactly: OCA, CM, cx_engineer, field_technician, design_engineer, owner_fm
---
## Mon May 26 23:50:35 CDT 2026 - US-006
- Implemented frontend lib/permissions module with canManageTeam and canCreateProject predicates
- Files changed:
  - Created frontend/src/lib/permissions.ts with two permission predicate functions
  - Created frontend/src/lib/__tests__/permissions.test.ts with 16 comprehensive test cases
- All 16 tests pass successfully under vitest (8 for each predicate)
- TypeScript type checking passes with npm run type-check
- Both predicates return true for 'OCA' and 'CM', false for all other roles and invalid inputs
- **Learnings for future iterations:**
  - Frontend permissions module follows same pattern as backend
  - Functions handle null and undefined defensively using type union Role | null | undefined
  - Test structure mirrors backend tests but with vitest describe/it/expect
  - Type imports use `import type { Role }` from roles module
---

## Mon May 26 23:51:00 CDT 2026 - US-007
- Widened role type unions to use the imported Role type from lib/roles module
- Files changed:
  - frontend/src/lib/supabase.ts: Changed Membership interface role field from 'OCA' | 'cx_engineer' to Role type
  - frontend/src/lib/members.ts: Changed getCurrentUserRole return type from 'OCA' | 'cx_engineer' | null to Role | null and updated the cast
- TypeScript type checking passes successfully
- getPrimaryOrg.ts file does not exist yet (to be created in future stories if needed)
- **Learnings for future iterations:**
  - The Role type is exported from frontend/src/lib/roles.ts and should be imported with `import type { Role }`
  - TypeScript checking command is `npm run type-check` not `npm run typecheck`
  - The 6-role model is now properly typed throughout the frontend type system
  - Some files referenced in PRDs may not exist yet, which is okay - skip them and focus on what does exist
---

## Mon May 26 23:57:00 CDT 2026 - US-008
- Implemented dynamic Role dropdown generation using ROLES array from lib/roles module
- Files changed:
  - frontend/src/app/(app)/project/[id]/members/page.tsx: Imported ROLES and ROLE_LABELS, replaced hardcoded <option> elements with ROLES.map()
- Role dropdown now shows all 6 canonical roles with proper display labels
- Default formData.role remains 'cx_engineer' as required
- TypeScript type checking passes successfully
- **Learnings for future iterations:**
  - Use ROLES and ROLE_LABELS from lib/roles for any role-related UI components
  - The map pattern for generating options keeps UI consistent with the source of truth
  - TypeScript check command in frontend directory is `npm run type-check`
---
